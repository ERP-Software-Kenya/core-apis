services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: core-backend
    restart: unless-stopped

    ports:
      - "10000:10000"

    env_file:
      - .env
